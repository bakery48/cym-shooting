/**
 * 全チューニング値の集約点。
 * ロジック中に数値をベタ書きしない（企画書 §8）。
 */
export const CONFIG = {
  runSeconds: 180,
  maxBreach: 10,

  // 報酬はインクの本数で決まる（剥がす手数がそのまま値段になる）
  kill: { perInk: 15, armoredMul: 4, bare: 5 },
  armoredChance: { base: 0.05, perSec: 0.0004 },
  // 白い敵（インクを持たない下位の敵）の出現率。装甲とは排他。
  bareChance: { base: 0.18, perSec: 0 },
  bareRadius: 0.78,          // 通常敵に対する半径比。小さいことで「下位」を示す

  // 湧きの密度カーブ。**毎秒の湧き数**を rate0 から rate1 へ、
  // 進行度の curve 乗で増やす（curve > 1 で後半ほど伸びる）。
  // 間隔（秒）で補間すると毎秒の数が終盤だけ跳ね上がる形になるので、
  // 「だんだん濃くなる」を素直に書けるほうを取っている。
  spawn: { rate0: 0.55, rate1: 6.0, curve: 2.0 },
  fall:  { start: 46,   rampPerSec: 0.55 },   // px/秒（縦640px基準）

  // 混色の導入。頭が追いつかないのを防ぐため、種類は時間で少しずつ増やす。
  mixed: {
    // 初登場したばかりの混色はゆっくり落ちて解読の時間を作り、
    // その色に慣れる頃（ラン終盤）には単色と同じ速さになる。
    slowStart: 0.55,     // 登場直後の落下速度（単色に対する比）
    // 何が混ざっているかを敵の上に点で示す。2色以上のときだけ出す。
    guide: { radius: 0.2, gap: 0.5, ring: 1.6 },
  },

  // 役割。照合の負荷ではなく「どこにいるか・いつ撃つか」を問うための軸。
  roles: {
    // 分裂: 撃破すると素地の小さい敵に割れる。高い位置で割るほど処理の時間ができる。
    // 破片は左下・右下へ**加速しながら**散る。等速で分かれるより
    // 「割れた」に見えるのと、開き方が時間とともに変わるので追う手が要る。
    split:   { fragments: 2, radiusMul: 0.6, spreadPx: 10, fallMul: 1.0,
               burst: { ax: 210, vxMax: 120, ay: 110, vyMax: 110, spin: 3.2, bounce: 0.8 } },
    // 群れ: 同じ単色がまとまって湧く。照合は増えず、位置取りだけが問われる。
    cluster: { min: 3, max: 5, gapMul: 2.3 },
    // 急降下: 遅く落ちてきて途中から加速する。「遅いうちに撃つか、後回しにするか」。
    // 予備動作（warnSec）は必須 ― 無いと「見ていたのに落ちた」で理不尽になる。
    dive:    { slowMul: 0.45, fastMul: 2.4, triggerY: 0.42, warnSec: 0.55 },
    // 運び屋: 突破されると突破カウントが余分に進む。優先順位を問う。
    carry:   { breachCost: 2, rewardMul: 2.5, radiusMul: 1.15, fallMul: 0.85 },
    // 増殖: 遅いが、放っておくと一定時間ごとに倍々に分かれる。
    // 「後回しにできない敵」を作るための役割で、問うのは**処理の順番**。
    // 必ず単色にする ― 倍々に増えるものの照合が重いと、詰みに直結する。
    // 世代ごとに報酬は半分。増やして稼ぐ／早く潰すのどちらでも総額は同じで、
    // 「稼ぐために泳がせる」が最適解にならないようにしてある。
    // jitter は「揃わせない」ためのもの。全員が同時に同じ幅で分かれると、
    // 左へ出た子と右へ出た子が同じ位置で出会って重なり、突き放した意味が消える。
    breed:   { fallMul: 0.45, interval: 3.4, jitter: 0.3, maxGen: 3, radiusMul: 0.86,
               gapPx: 34, value: 2,
               // 分かれぎわに左右へ突き放す。その場で倍になるだけだと
               // 重なったまま並ぶので、1発の射線でまとめて落ちてしまう。
               // genScale: 世代が進むほど押し幅を狭める。等幅で押すと
               // 「左へ出た子」と「右へ出た子」がちょうど同じ位置で出会って重なる
               // （-d+d = +d-d）。幅を変えれば、その一致が起きない。
               kick: { ax: 420, vxMax: 95, genScale: 0.55, accelSec: 0.4, jitter: 0.5,
                       decay: 2.6, bounce: 0.8 } },
  },

  // 落ち方のバリエーション。形を統一したぶん、差は挙動で付ける。
  motion: {
    leaf:  { swayHz: 0.55, swayPx: 46, fallMul: 0.78, spin: 2.4 },
    // 弾を見て避けるのではなく、**自機と同じ縦軸に居続けないように**
    // じりじり横へずれる。keepX まで離れたら止まる（常に逃げ続けない）。
    dodge: { fallMul: 0.62, speed: 58, keepX: 118 },
  },

  ship: {
    // 発射間隔（秒）。連射は控えめにして、1発ずつ狙う感じを出す。
    // 3つの値は比を保って動かすこと ― base だけ変えると連射速度の強化が
    // 相対的に効かなくなる（現状は max まで買うと 2.4 倍速で、比は元のまま）。
    fireBase: 0.60, fireMin: 0.20, firePerLv: 0.07,
    bulletSpeed: 560,
    keyboardSpeed: 420,   // px/秒（縦640px基準）: キー移動の速度
    edgeMargin: 24,
  },
  pod: {
    // 的の有無にかかわらず撃ち続けるので、間隔は自機より遅くしておく。
    // range は僚機（狙って撃つ）の射程としてだけ使う。
    orbitRadius: 58, orbitSpeed: 0.9, fireMul: 1.15, bulletSpeed: 460, range: 520,
    // 公転の航跡。買ったポッドが「回る帯」として見えるようにするためのもの。
    // 帯の長さは interval × samples × orbitSpeed（ラジアン）で決まる。
    // 既定は約2.4秒 = 公転の約120度で、3基そろうとほぼ輪になる。
    // 長さを変えたいときは samples ではなく interval を動かす ―
    // samples は描画するセグメント数そのものなので、伸ばすと描画コストが増える。
    trail: { samples: 28, interval: 0.085, width: 4.0, alpha: 0.55 },
    // 増設すると公転半径を少しずつ広げる。基数が増えても団子にならず、
    // 「輪が育つ」ことがそのまま強さの表示になる。
    ringPerPod: 0.07, ringMax: 1.75,
  },

  // 恒久強化（コア）。ラン内の弧に触れない範囲だけを受け持つ。
  meta: {
    coreClear: 3,        // クリアで得られるコア
    coreFirstClear: 5,   // 初クリアの追加ぶん
    coreFail: 1,         // 失敗しても最低限は得られる
    wing:   { offsetX: 40, offsetY: 16, fireMul: 2.4, bulletSpeed: 460, range: 460 },
    bullet: { radiusPerLv: 0.35 },   // 弾芯1段ごとの自機弾の半径増加率
  },

  tapThreshold: 10,       // これ未満の移動なら「タップ＝砲塔回転」

  // 縦シューの見え方を保つため、プレイ領域は縦長に制限して中央に置く。
  // 横に広いデスクトップ画面でも体感を揃える（企画書 §8 のS基準と同じ考え方）。
  stage: { aspect: 0.78, minWidth: 300 },

  costs: {
    // ポッドは1基では2色以上の敵を倒し切れず、そろって初めて自動化が完成する。
    // 価値が非線形なので、以前ほど段階的に高くしない。
    pod: { C: 210, M: 140, Y: 300 },
    // ポッドは色ごとに増設できる。2基目以降はこの倍率で高くなる。
    // 上限を作らないのは、終盤に金の使い道が尽きると手数が伸びなくなるため。
    podMul: 1.9,
    rate:   { base: 130, mul: 1.65, max: 5 },
  },
};

/* ============================================================
   減法混色
   ------------------------------------------------------------
   敵は「まだ乗っているインク」を3ビットで持つ。撃たれた色のインクが剥がれ、
   全部剥がれたら撃破。順番は問わない（赤にMを当てれば黄、Yを当てればマゼンタ）。

     enemy.inks &= ~bullet.ink;
     if (enemy.inks === 0) 撃破

   インクを1本も持たない敵（BARE）は剥がすものが無いので1発で割れる。
   ============================================================ */
export const INK = { C: 1, M: 2, Y: 4 };
export const BARE = 0;

/** 砲塔が回る順。段階解放ではこの並びの先頭から順に開いていく。 */
export const INK_ORDER = [INK.M, INK.C, INK.Y];

export const INK_NAME = { [INK.C]: 'シアン', [INK.M]: 'マゼンタ', [INK.Y]: 'イエロー' };
export const INK_ID   = { [INK.C]: 'C', [INK.M]: 'M', [INK.Y]: 'Y' };

/**
 * インクの組み合わせごとの表示色。
 * 物理的な乗算そのままだと青と黒が背景（#232a45）に沈むので、
 * 色相の関係は保ったまま明度だけ上げてある。黒は縁を描いて浮かせる（§7）。
 */
export const COLOR = {
  0: '#e8edff',   // 素地（白）
  1: '#2fe4f0',   // C
  2: '#ff53d6',   // M
  3: '#6a5cff',   // C+M = 青
  4: '#ffe23c',   // Y
  5: '#4ddb62',   // C+Y = 緑
  6: '#ff5a45',   // M+Y = 赤
  7: '#171c2b',   // C+M+Y = 黒
};

export const INK_COUNT = [0, 1, 1, 2, 1, 2, 2, 3];

export const PALETTE = {
  bg: '#232a45',
  line: '#4a5490',
  armor: '#e8edff',
  bad: '#ff4d5e',
  deflect: '#7a86b8',
  armorSpark: '#c9d2f0',
  hull: '#c9d2f0',
  rim: '#8b93b8',      // 黒い敵を背景から浮かせるための縁
};
