# Steam販売までの計画

作成: 2026-09-05
対象: `docs/game-spec.md` の企画をSteamで売れる形にするまで

この文書は「何が足りないか」を正直に並べるためのもの。
実装の指針ではなく、判断が必要な項目の一覧として使う。

---

## 0. 最初に直視すべきこと

企画書の設計は**スマートフォンのブラウザで3分遊ぶ**ことを前提に組まれている。
Steamは有料PCゲームの店で、購入者は数時間ぶんの内容量を期待する。
つまりSteam化とは移植作業ではなく、**遊べる総量を作る作業**になる。

現状で不足しているのは技術ではなく分量:

| | 現状 | 有料Steamタイトルの実質的な下限 |
|---|---|---|
| プレイ時間 | 1ラン3分・モード1つ | 2〜5時間ぶんの目的 |
| 繰り返す理由 | なし（ラン間の永続進行はスコープ外） | 実績・記録更新・アンロック等 |
| 見た目 | 図形のみ・アセットなし | ストア画像とトレーラーは必須 |
| 音 | なし | 最低限のSE。無音は返金理由になる |

技術面（デスクトップ操作・ウィンドウ・パッケージング）は済んでいる。
残りはすべて**内容量と体裁**の作業になる。

---

## 1. 済んでいるもの

- [x] プロトタイプのモジュール分割（企画書 フェーズ1）
- [x] キーボード / マウス操作（Steam＝PCなので必須）
- [x] 16:9 デスクトップ解像度でのレイアウト（盤面は縦長のまま中央、HUDは右）
- [x] 中断（Esc / P）― ウィンドウのゲームには必須
- [x] Electron シェルと `electron-builder` 設定（Steam depot 向けに展開済みディレクトリ出力）
- [x] 60fps の確認（敵40体・弾150発で 16.6ms/frame）
- [x] スマホ対応の確認（縦持ち・横持ち、タッチでの回転と移動）
- [x] 単一HTMLへのまとめ（`npm run build:single`）― スマホに送ってそのまま開ける

---

## 2. 内容量（最大の作業。ここが未決定）

企画書 §10 フェーズ3で「意図的に決めていない」とされている項目が、
そのままSteam化の可否を決める。**実装前に判断が要る。**

### 2-1. ラン間の永続進行 → **決定: ステージ / モード制 ＋ 永続する所持金と強化**

当初は「永続強化は入れない」で確定していたが、**2026-09-05 に方針変更**。
所持金と強化はステージをまたいで残る（区を抜けても戻らない）。

**この変更で開いた宿題が2つある。どちらも実プレイでの確認が要る。**

1. **1ラン内の弧が2周目以降は消える。** 企画書 §2 のコアループは
   「全手動 → 部分自動 → 手が装甲へ」の変化が1ラン3分で起きることだった。
   最初から自動化済みで始まると、この変化が起きない。
   面ごとに難度を上げて「持ち込んだ強化でもまだ足りない」状態を作るか、
   面ごとに要求を変えて別の強化を買わせるか、判断が要る
2. **経済が行き止まる。** 全強化を上限まで買う総額は **6,700**。
   1ランの収入がおよそ 700〜900 なので、**数ランで買い切って所持金の意味が消える**。
   上限を上げるか、強化を増やすか、消費先を作るかの判断が要る
   （企画書 §6 の原則があるので、増やすなら「画面上で何が増えるか」を先に答えること）

実装済み（`js/stages.js`）:
- 区6つ（均等 / 圧 / 偏り / 装甲 / 密集 / 完走）
- モード2つ（手動限定 / 全自動）― 第2区クリアで開放
- 前の区をクリアすると次が開く。**自機の性能は面をまたがない**
- 保存するのは開放状況と自己ベストのみ（`js/core/save.js`）

残っている作業:
- [ ] 各面のバランス調整（現状は未プレイテストの初期値）
- [ ] 面数が足りるかの判断。8面で足りなければ追加するが、
      **面を増やす前に1面ずつの密度を上げるほうが効く**
- [ ] 面ごとの見た目の差（現在は数値だけが違い、画面は同じ）

### 2-2. 尺（面ごとに設定）

2-1 の決定にあわせ、尺は面ごとの変数にした（2分〜5分）。
基準は3分のまま。実プレイでの調整が残っている。

### 2-3. 複合敵（複数の弾種を要求する敵）

出すなら中盤以降の変化として効く。出さないなら3分の弧を別の手段で作る必要がある。

---

## 3. 体裁（内容が決まってから着手）

### 音 ― 実装済み
`js/core/audio.js`。音源ファイルは持たず Web Audio API で合成する。
回転・撃破・装甲撃破・弾かれ・装甲弾き・購入・突破・クリア・失敗の9種。

- 弾種の ●▲■ はド・ミ・ソに対応させ、音でも種類が分かるようにした
- 同時発音数に上限を設け、撃破が連続しても飽和しないようにしている
- 音が出せない環境でも例外を投げず、ゲームは続行する
- 消音（<kbd>M</kbd>）と音量は保存される

残り: BGM は未着手。効果音より優先度は低い。

### ストア用画像
ゲーム本体は画像アセットを使わない方針だが、**ストアページは別**で、
規定サイズの画像を出さないと審査を通らない。
図形と3色のパレットをそのまま使えば、絵が描けなくても成立させられる。

必要なもの（サイズは Steamworks の最新ドキュメントで必ず確認すること。変更される）:
- ヘッダーカプセル / メインカプセル / 縦型カプセル
- ライブラリ用画像（ライブラリ表示・ヒーロー画像・ロゴ）
- スクリーンショット複数枚
- トレーラー動画（実質必須。ゲーム画面の録画で足りる）

### ローカライズ
UIの文字数は少ない。日本語と英語の2言語は費用対効果が高い。
現在は文字列がHTMLとJSに直書きなので、切り出す作業が要る。

---

## 4. Steamworks 側の作業

順序に依存関係があるので早めに着手する。金銭と本人確認が絡む部分は時間がかかる。

1. **Steamworks への登録と Steam Direct の手数料支払い**
   （1アプリごとの手数料。一定の売上に達すると返金される仕組み。金額と条件は公式で確認）
2. **銀行口座・税務情報の登録** ― 本人確認が要るため最も待たされる。最初にやる
3. **ストアページの作成と審査**
4. **「近日登場」ページの公開** ― Steamは発売日より前に一定期間ストアページを
   公開しておくことを求めている。期間は公式の要件を確認し、逆算して計画する
5. **ビルドのアップロード（depot）** ― `npm run dist` の出力をそのまま上げる
6. **年齢レーティング / コンテンツ申告**
7. **リリース**

### 実績・リーダーボード（任意）
Steamworks SDK との連携が要る。Electron から使う場合はネイティブモジュールが必要で、
`steamworks.js` などの選択肢がある。導入する場合はビルドがプラットフォームごとに
分かれるので、内容（2-1）が固まってから判断する。

---

## 5. 技術面で残っているもの

- [ ] ゲームパッド対応（Steam的にはほぼ期待される。Gamepad API で足りる）
- [x] 音量・消音の設定と保存
- [x] ベスト記録の保存（`localStorage`）
- [ ] ウィンドウ / フルスクリーン切り替え
- [ ] 企画書 §10 フェーズ2 の仮説検証
  1. タップ回転のコストが緊張として機能しているか
  2. 貫通・拡散が「強くなった」として体感できているか
- [ ] Windows / macOS 向けビルドの実機確認（現在Linuxでのみ確認済み）

---

## 6. 進める順序（提案）

内容量の判断（§2）が全体の前提になるので、そこから始める。

1. ~~§2-1 を決める~~ → ステージ / モード制で決定、実装済み
2. **各面のバランス調整とフェーズ2の仮説検証** ← 今ここ。
   「手動限定」モードが仮説1（回転コストが緊張として機能しているか）の
   測定にそのまま使える
3. ~~音の実装~~ → 効果音は実装済み
4. 面の密度を上げる。足りなければ面を追加する
5. ゲームパッド・フルスクリーン切り替え
6. ストア用画像とトレーラー
7. Steamworks 登録（税務情報だけは早めに着手してよい）
